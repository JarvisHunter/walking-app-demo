## Packages
framer-motion | Smooth animations and playful gamified interactions
canvas-confetti | Delightful celebration effect when users join the early access
@types/canvas-confetti | TypeScript types for canvas-confetti
lucide-react | Already in stack, but explicitly noting for iconography

## Notes
Tailwind Config - extend fontFamily in theme:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
